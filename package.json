{
  "name": "ts-template",
  "version": "1.0.0",
  "main": "Bootstrap.ts",
  "scripts": {
    "run:dev": "ts-node-dev --respawn --transpile-only --no-notify -r tsconfig-paths/register src/Bootstrap.ts",
    "test:repository": "jest ./User.repository.spec.ts --watch",
    "test:service": "jest ./User.service.spec.ts --watch",
    "test:controller": "jest ./User.controller.spec.ts --watch",
    "test:final": "jest ./Bootstrap.spec.ts --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "dotenv": "^8.2.0",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.3",
    "ts-node-dev": "^1.1.6",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.2.2"
  }
}
